apiVersion: v1
kind: Service
metadata:
  name: services-ui
  namespace: services-system
  labels:
    app: services-ui
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: services-ui
